import Login from "@/components/Login";

const Home = () => {
  return (
    <div className="flex justify-center items-center w-full h-[100vh]">
      <Login />
    </div>
  );
};

export default Home;
